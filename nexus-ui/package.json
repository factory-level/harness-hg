{
    "name": "nexus-ui",
    "version": "0.1.0",
    "private": true,
    "description": "The rebuilt Nexus frontend (ADR 0157, #666) - Astryx components + custom canvas over the _docs/design/nexus-ui/ spec. Builds the plugin IIFE with React staying the host SDK's (src/shims/).",
    "type": "module",
    "scripts": {
        "typecheck": "tsc --noEmit",
        "test": "bun test",
        "build": "vite build",
        "build:standalone": "vite build -c vite.standalone.config.ts",
        "build:demo": "vite build -c vite.demo.config.ts",
        "gen-theme": "bun scripts/gen-theme.ts",
        "browser-test": "NX_BROWSER=1 bun test browser/"
    },
    "dependencies": {
        "@astryxdesign/core": "^0.5.0",
        "@astryxdesign/theme-neutral": "^0.5.0",
        "@stylexjs/stylex": "^0.15.4",
        "lucide-react": "^1.34.0"
    },
    "devDependencies": {
        "@types/bun": "^1.4.0",
        "@types/react": "^19",
        "@types/react-dom": "^19",
        "playwright-core": "^1.62.1",
        "react": "^19",
        "react-dom": "^19",
        "typescript": "^5.5",
        "vite": "^7"
    }
}

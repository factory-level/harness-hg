<!-- GENERATED by infra/scripts/generate-cli-docs.py — do not edit by hand.
     Run `make cli-docs` after changing cli/src/commands.ts; `make cli-docs-drift` fails on stale. -->

# CLI reference

**What this page tells you:** every command `hg` accepts, and where each one's full page is.

These pages are generated from the same manifest the CLI builds its parser from. They cannot
describe a flag that does not parse. If a command here does not work, the bug is in the CLI.

`hg` is an operator tool. Nothing in the running system calls it.

## Reading the output

- **`--json` prints exactly one JSON document on stdout.** Narration goes to stderr. That is
  the contract for scripting.
- **A proof command emits a `ProofResult`:** one row per numbered check. A check that could
  not run reads `unknown`. It never reads as a pass.

See [Testing](../../platform/testing.md) for what the proofs establish.

## Commands


## Start here — one front door per loop

| Front door | Loop | What it does |
|---|---|---|
| [`hg dev`](dev.md) | dev | start the hot-reload REPL (cold: onboard → up → dev) |
| [`hg env new`](env.md) | ops | day-0 from the root-of-trust coordinates: stacks init'd, config generated, pulumi up, hand-off (#676) |
| [`hg bundle init`](bundle.md) | agent-bundle | scaffold a new agent-bundle repo and prove it validates |

## dev: build and run a team locally

| Command | Journeys | `--json` | What it does |
|---|---|---|---|
| [`hg onboard`](onboard.md) | dev | — | register a repo, or one agent, with the local loop |
| [`hg up`](up.md) | dev | — | start the local loop: cluster, Argo CD, Grafana, git remotes, every onboarded agent |
| [`hg dev`](dev.md) | dev | — | watch loop: an edit commits, renders and syncs through the real Git → Argo CD path |
| [`hg validate`](validate.md) | dev · agent-bundle | yes | the contract gate, no cluster: every failure at once |
| [`hg test`](test.md) | dev | yes | run the acceptance tiers against every agent |
| [`hg logs`](logs.md) | dev · ops | yes | container logs, crash context and Argo conditions for one pod |
| [`hg prompt`](prompt.md) | dev · agent-bundle | yes | send one prompt to a deployed agent |
| [`hg dash`](dash.md) | dev · ops | yes | declared dashboards and alerts vs what Grafana actually imported |
| [`hg debug`](debug.md) | dev · ops | yes | the passive debug observer |
| [`hg expose`](expose.md) | dev | — | refresh or stop the auto-started local URLs |
| [`hg down`](down.md) | dev | — | stop every host-side process (cluster untouched) |
| [`hg open`](open.md) | dev | yes | every profile's console/service URLs |
| [`hg reset`](reset.md) | dev | — | tear the local environment down |
| [`hg status`](status.md) | dev | yes | the local environment's state: profiles, ports, exposures |
| [`hg identity`](identity.md) | dev | yes | the local identity provider's issuer, clients and users |

## ops: host your own environment

| Command | Journeys | `--json` | What it does |
|---|---|---|---|
| [`hg platform`](platform.md) | ops | yes | the environment backup: create, schedule, fetch, restore, verify, prove |
| [`hg backup`](backup.md) | ops · agent-bundle | yes | app-owned backup routines: list, run, verify, export, restore |
| [`hg launch`](launch.md) | ops | yes | the whole acceptance matrix for an environment, aggregated |
| [`hg reconcile`](reconcile.md) | ops | yes | the destination host's reconcile timer |
| [`hg event`](event.md) | ops · agent-bundle | yes | the event router: list, trace, publish, prove the queue, dead letters |
| [`hg communication`](communication.md) | ops | yes | the communication acceptance matrix as one command |
| [`hg discord`](discord.md) | ops · dev | yes | the Discord gateway, observed from outside (Hermes-native; Eve's Discord is the connection gateway's) |
| [`hg cron`](cron.md) | ops · dev | — | the agent's scheduler surface - Hermes wrapped natively, Eve projected read-only |
| [`hg edge`](edge.md) | ops | yes | the tunnel and access policy: prove, publish, unpublish, list |
| [`hg envfile`](envfile.md) | ops | — | profile environment files: list, use, set, unset (values never print) |
| [`hg env`](env.md) | ops | yes | the environment spec → generated Pulumi stack config |
| [`hg agent`](agent.md) | ops · dev | yes | what the platform resolved for an agent, and what the running one reports |
| [`hg connection`](connection.md) | ops | yes | third-party app connections: list, plan, set, prove |
| [`hg workspace`](workspace.md) | ops · agent-bundle | yes | repository mounts: plan, list, verify, doctor, test |
| [`hg server`](server.md) | ops | yes | destination server lifecycle: preflight, bootstrap, register, destroy |
| [`hg grafana`](grafana.md) | ops | yes | prove the promised Grafana panels exist |
| [`hg auth`](auth.md) | ops | yes | prove sign-in and RBAC on the control plane |
| [`hg slack`](slack.md) | ops | yes | prove the provisioned Slack surface: apps, secrets, channels, events edge |
| [`hg harness`](harness.md) | ops | yes | the declared harnesses |

## agent-bundle: author an Agent Team Repo, no cluster

| Command | Journeys | `--json` | What it does |
|---|---|---|---|
| [`hg eval`](eval.md) | agent-bundle · dev | yes | run the repo's eval suite; publish and read back results |
| [`hg gitops`](gitops.md) | agent-bundle | yes | verify or upgrade a destination repo |
| [`hg topology`](topology.md) | agent-bundle | yes | compile the declarations against an environment, no cluster |
| [`hg observability`](observability.md) | agent-bundle | yes | the observability overlay: inventory and proof |
| [`hg nexus`](nexus.md) | agent-bundle | yes | the Nexus UI plan compiler |
| [`hg chatops`](chatops.md) | agent-bundle | yes | ChatOps spaces: list, plan, render, tail, test |
| [`hg bundle`](bundle.md) | agent-bundle | yes | front door: scaffold a new Agent Team Repo |

## meta

| Command | Journeys | `--json` | What it does |
|---|---|---|---|
| [`hg help`](help.md) | all | — | full flags and subcommands for one command |

## Environment variables

| Variable | What it does |
|---|---|
| `CLOUDFLARE_API_TOKEN` | The Cloudflare API token used by the `edge` verbs. |
| `HERMES_GITOPS_HOME` | The CLI's state directory. Default `~/.hermes-gitops`. |
| `HG_BACKUP_WRITER_SA` | Service account impersonated to WRITE platform backups to the sink. |
| `HG_CF_ACCESS_CLIENT_ID` | Cloudflare Access service-token id, when the control plane sits behind Access. |
| `HG_CF_ACCESS_CLIENT_SECRET` | Cloudflare Access service-token secret. |
| `HG_CLUSTER_NAME` | The local loop's k3d cluster (default `hermes-gitops-cli`). Set it with HERMES_GITOPS_HOME to run a second, isolated loop beside the default one; `reset --nuclear` deletes only this cluster. |
| `HG_DISCORD_API_BASE` | Overrides the Discord API base, for testing against a fake. |
| `HG_DISCORD_BOT_TOKEN` | Bot token for the live ChatOps legs of `communication prove`. |
| `HG_DISCORD_SANDBOX_CHANNEL` | Channel id the live ChatOps legs are allowed to post into. |
| `HG_EVAL_TOKEN` | The Nexus eval publish token. Read from the environment or `--token-file`, never from argv, so it cannot land in shell history. |
| `HG_GRAFANA_ANON_VIEWER` | Treat Grafana as anonymously readable when proving observability. |
| `HG_KUBE_CONTEXT` | Overrides the kube context (default `k3d-<cluster>`). |
| `HG_PROOFS_DIR` | Where `ProofResult` documents are written. |
| `HG_RECONCILE_LOCK` | Path to the reconciler's flock. A run is skipped, not queued, while it is held. |
| `HG_RESTORE_READER_SA` | Service account impersonated to READ backups back. Separate from the writer on purpose — restore is verified with an identity that cannot have written the backup. |

No command takes a secret as an argument. A credential comes from the environment or from a
file named by a flag. See [Secrets](../../platform/secrets.md).

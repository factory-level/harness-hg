<!-- GENERATED by infra/scripts/generate-cli-docs.py — do not edit by hand.
     Run `make cli-docs` after changing cli/src/commands.ts; `make cli-docs-drift` fails on stale. -->

# hg agent

**What this page tells you:** every `hg agent` subcommand, with its arguments, flags and defaults.

What the platform resolved for an agent, and what the running one reports.

## Synopsis

```text
hg agent show [flags]
hg agent inspect [flags]
hg agent render --output <dir> [flags]
hg agent apply [flags]
hg agent exec -- <args...> [flags]
hg agent prove [flags]
hg agent evals [<eval-id>...] [flags]
```

`--json` prints one JSON document on stdout; narration goes to stderr.

## Description

Engine-neutral: every sub dispatches on the profile's runtime. `inspect` and `render` are OFFLINE and need no cluster — they read the same value documents the ApplicationSet layers onto the chart and build the agent runtime manifest from them, so they are safe in CI and `render` is byte-identical run twice. `show` adds what only the running pod knows.

## `hg agent show`

```text
hg agent show [flags]
```

As configured in the pod — hermes: model/skills/plugins/mcp/cron; eve: model/channels/subagents/schedules/tools.

| Flag | Value | Default | What it does |
|---|---|---|---|
| `--profile` | `<name>` | — | Narrow to one profile; default is every onboarded profile. |

## `hg agent inspect`

```text
hg agent inspect [flags]
```

Offline: the resolved runtime manifest — engine, source revision, workspaces, required secret names, connections.

| Flag | Value | Default | What it does |
|---|---|---|---|
| `--profile` | `<name>` | — | Narrow to one profile; default is every onboarded profile. |
| `--gitops` | `<clone>` | — | Read the overlay from a GitOps checkout instead of the local staging tree. |

## `hg agent render`

```text
hg agent render --output <dir> [flags]
```

Offline: write the record and the runtime manifest for one profile — deterministic, ci-safe.

| Flag | Value | Default | What it does |
|---|---|---|---|
| `--profile` | `<name>` | — | Narrow to one profile; default is every onboarded profile. |
| `--output` | `<dir>` | — | Directory to write profile.yaml + runtime-manifest.json into (created if absent). |
| `--gitops` | `<clone>` | — | Read the overlay from a GitOps checkout instead of the local staging tree. |

## `hg agent apply`

```text
hg agent apply [flags]
```

Activate declared cron — the same `hermes cron sync` the chart's boot script runs (hermes only).

| Flag | Value | Default | What it does |
|---|---|---|---|
| `--profile` | `<name>` | — | Narrow to one profile; default is every onboarded profile. |
| `--dry-run` | — | — | Print what would change without applying. |

## `hg agent exec`

```text
hg agent exec -- <args...> [flags]
```

Debug escape hatch: `hermes` in the pod (hermes) or `eve` in the project dir (eve).

| Flag | Value | Default | What it does |
|---|---|---|---|
| `--profile` | `<name>` | — | Narrow to one profile; default is every onboarded profile. |

## `hg agent prove`

```text
hg agent prove [flags]
```

The agent-runtime acceptance matrix, per harness.

Partitions the selection by harness and runs each harness's own matrix — one merged ProofResult. Eve: EVE001..011 platform legs (health, Workflow callback route, Basic challenge, an authenticated real turn (`unknown` on a dev-placeholder credential), byte-identical record render) plus the documented eve channel contract; EVE012..020 behaviours around the routes (cancel on the stream, --deep durability across a pod restart, compaction, schedules firing in-process, subagents + a delegation eval, the backup ledger and a --deep restore round-trip, workspace bindings at the resolved sha, child Applications Synced+Healthy); EVE022..023 the mounted runtime manifest equals the offline resolve and `hg agent show` answers. Hermes: HRM001 the running pod answers `agent show` with a coherent snapshot and every declared cron job is activated; HRM002 the smoke tier passes — deliberately shallow, deep Hermes acceptance stays `hg test`. A leg whose precondition is absent reports `unknown`, never a pass. `hg launch prove` aggregates this subject whenever any agent is onboarded.

| Flag | Value | Default | What it does |
|---|---|---|---|
| `--profile` | `<name>` | — | Narrow to one profile; default is every onboarded profile. |
| `--deep` | — | — | Also run EVE013 (pod restart) and EVE018 (backup/restore round-trip) - minutes, and the pod rolls. |

## `hg agent evals`

```text
hg agent evals [<eval-id>...] [flags]
```

Run an eve agent's own eve evals (evals/ beside agent/) against its deployment.

Runs `eve eval --url` from inside the agent's pod, against the served process, with the platform's minted credential - eve's own acceptance mechanism (docs/evals), needing no local toolchain. Positional ids select evals by id or directory prefix. Exit code is eve's: 0 every gate passed, 1 a failure, 2 a configuration error. Refuses on a Hermes selection - the frozen legacy harness has no such mechanism; its behaviour suite is `hg eval`.

| Flag | Value | Default | What it does |
|---|---|---|---|
| `--profile` | `<name>` | — | Narrow to one profile; default is every onboarded profile. |
| `--strict` | — | — | Soft below-threshold assertions also fail (eve eval --strict). |

## See also

- [Command index](index.md)
- [Agent runtime](../contracts/agent-runtime.md)
- [Runtime eve](../../platform/runtime-eve.md)

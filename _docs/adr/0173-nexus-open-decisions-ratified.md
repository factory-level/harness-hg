# 0173 — The seven Nexus open decisions are ratified as their defaults

**Decision.** The seven operator decisions deferred from the #665/#677 sign-offs (#712) are
answered (Calvin, 2026-08-27), and every answer ratifies the recorded default the rebuild
shipped on: tabs retained with Workspace as the landing view (#541/#543 collapse stays a
possible later change); plane BADGES on every panel frame, grouping/alert-row marks deferred
(#706); the browser suite proves the anonymous-viewer embed posture with SSO proofs staying in
the deploy-proof orbit (#300/#332/#703); `sheet:*` rollups stay dropped from the overlay until
a sheet-tab affordance wants them; Backups keeps bundle rollups for workloads with the control
plane always explicit per-component (#582); #544 closes as satisfied by #545's mandala; Alert
Routing keeps the honest in-memory history window until #295. The two parked sign-off boxes —
the #665 anti-pattern list and the #677 hierarchy — are signed off as built. The
`interactions.md` open-decisions section flips from questions to the decision record.

**Reason.** The rebuild proceeded on recorded defaults under the operator's defer-questions
directive; ratification converts provisional defaults into decided spec, which is what the
sign-off boxes existed to establish.

**Cost.** None to the shipped system — no default changes. The deferred alternatives (nav
collapse, plane grouping, sheet-tab dots, durable history) remain unowned until someone wants
them; naming them here is what keeps their absence deliberate rather than forgotten.

{
    "name": "hermes-gitops-cli",
    "version": "0.1.0",
    "description": "The hg operator CLI for authoring agent teams, running a local dev loop and hosting Harness Hg environments.",
    "type": "module",
    "bin": {
        "hermes-gitops": "src/main.ts",
        "hg": "src/main.ts"
    },
    "scripts": {
        "typecheck": "tsc --noEmit",
        "test": "bun test"
    },
    "dependencies": {
        "ajv": "^8",
        "yaml": "^2.9.0"
    },
    "devDependencies": {
        "@types/bun": "latest",
        "typescript": "^5.5"
    }
}

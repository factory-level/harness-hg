# control-plane/loki/

**Purpose.** Control-plane log aggregation — NOT BUILT YET.

**Chart / machinery.** arrives with #661 (chart pinned in versions.json, deployed via the normal GitOps path).

**Signals.** will aggregate both planes split by the #660 plane label. Coverage is inventoried per component in #662.

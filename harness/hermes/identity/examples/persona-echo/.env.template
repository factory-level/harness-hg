# Environment variables required by the persona-echo distribution.
# Copy to `.env` and fill in your own values before running.

# Anthropic API key — persona-echo's model provider
# (required)
ANTHROPIC_API_KEY=

# Example config.yaml for persona-echo. Real distributions typically tune
# model/provider/toolset selection here; kept minimal for this example —
# see distribution.yaml's env_requires for the one secret this persona
# actually needs (ANTHROPIC_API_KEY, supplied via .env — see .env.template).
model:
  provider: anthropic
  name: claude-sonnet-4-5

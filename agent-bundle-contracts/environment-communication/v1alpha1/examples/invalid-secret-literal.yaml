# No credential literal has a home in this file - unknown keys reject, so
# a token pasted where a reference belongs fails validation instead of
# entering Git.
version: 1
chatopsConnections:
  company_discord:
    provider: discord
    token: xoxb-not-a-reference

# Every provider except recording requires a credentialRef - a real
# provider with no credential fails closed at authoring time, not at
# delivery time.
version: 1
chatopsConnections:
  company_discord:
    provider: discord

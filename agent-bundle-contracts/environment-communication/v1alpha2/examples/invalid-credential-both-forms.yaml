# A credentialRef is exactly one of {name,key} (in-cluster Secret) or
# {env} (operator host) - both at once is ambiguous.
version: 1
chatopsConnections:
  company_discord:
    provider: discord
    credentialRef:
      name: company-discord
      key: bot-token
      env: HG_DISCORD_BOT_TOKEN

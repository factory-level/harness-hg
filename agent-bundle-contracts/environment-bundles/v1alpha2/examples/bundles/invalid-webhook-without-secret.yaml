# INVALID: a webhook port with no env Secret. The router signs every
# delivery with the target's WEBHOOK_SECRET, which lives in that Secret -
# without it the agent cannot verify what it receives, so the binding
# would be unauthenticated by construction.
version: 2
bundles:
  - name: marketing-core
    profiles:
      - name: manager
        webhookPort: 8644

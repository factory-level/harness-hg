# INVALID: `tracing` is not one of the seven source kinds. The enum is closed
# on purpose - every consumer switches on it exhaustively, so a kind nobody
# knows renders as nothing at all, which is the one failure mode this whole
# document exists to prevent. A new source is a new version directory.
apiVersion: nexus.hermes.ai/v1alpha1
kind: RuntimeOverlay
observedAt: "2026-07-31T09:14:00Z"
stale: false
sources:
  argocd: { kind: argocd, status: configured, level: healthy, summary: all synced }
  grafana: { kind: grafana, status: not configured, level: unknown, summary: not configured }
  uptime: { kind: uptime, status: not configured, level: unknown, summary: not configured }
  eval: { kind: eval, status: not configured, level: unknown, summary: not configured }
  backup: { kind: backup, status: not configured, level: unknown, summary: not configured }
  communication: { kind: communication, status: not configured, level: unknown, summary: not configured }
  reconciliation: { kind: tracing, status: configured, level: healthy, summary: spans flowing }
components: {}
instances: {}
rollups: {}

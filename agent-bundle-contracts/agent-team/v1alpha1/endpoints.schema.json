{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hermes-gitops.factorylevel.dev/schemas/agent-team/v1alpha1/endpoints.schema.json",
  "title": "Agent endpoints (agents/<harness>/<name>/harness-hg/endpoints.yaml)",
  "description": "`agents/<harness>/<name>/harness-hg/endpoints.yaml` - what this agent makes reachable and what it receives: its typed endpoints (name, port, path, who may consume it), the verified external webhook inputs it accepts, and the routes from those inputs to agent handlers and ChatOps spaces. Written by the team. Read by the topology compiler (endpoint deployment records, capability providers, the communication plane) and the emitter (the Hermes-only `expose` block). Absent: the agent exposes nothing, provides no capability and receives no external input. Top-level keys starting with `x-` are anchor scratch space, ignored by every reader.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "apiVersion",
    "kind"
  ],
  "$defs": {
    "capabilityName": {
      "type": "string",
      "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
      "maxLength": 63,
      "description": "DNS-label capability name. Unique per provider within the compiled catalogue at a given locality."
    },
    "eventType": {
      "type": "string",
      "pattern": "^[a-z0-9]+(\\.[a-z0-9-]+)*/v[0-9]+$",
      "maxLength": 120,
      "description": "Versioned event type, e.g. observability.alert/v1. The version suffix is part of the contract: changing the payload shape is a new event type, never a silent widening."
    },
    "chatopsSpace": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_-]*#\\S+$",
      "maxLength": 200,
      "description": "A ChatOps destination as <connection-alias>#<opaque-destination>. The alias resolves to a provider plugin + credentials in environment/communication.yaml; everything after the first # is passed to that plugin verbatim (a Discord channel ID, a Slack destination, ...). Referencing a space here IS its registration - no separate registry exists."
    },
    "sessionPolicy": {
      "type": "object",
      "description": "How deliveries to an agent handler map onto Hermes sessions. Default when the target declares none: per-event.",
      "additionalProperties": false,
      "required": [
        "mode"
      ],
      "properties": {
        "mode": {
          "type": "string",
          "enum": [
            "per-event",
            "keyed",
            "route"
          ],
          "description": "How deliveries map onto Hermes sessions: per-event = a fresh session per event; keyed = one session per value of `key` (an incident, a workflow); route = one session for the whole route. The generated session key always carries <environment>/<profile>/<route>."
        },
        "key": {
          "type": "string",
          "pattern": "^(subject|data\\.[^.\\s]+(\\.[^.\\s]+)*)$",
          "description": "REQUIRED when mode is keyed: the envelope field the session groups by - `subject`, or a dot-path into the payload (`data.labels.incident`)."
        }
      },
      "if": {
        "properties": {
          "mode": {
            "const": "keyed"
          }
        }
      },
      "then": {
        "required": [
          "key"
        ]
      }
    },
    "deliveryPolicy": {
      "type": "object",
      "description": "Delivery semantics for one edge: transport mode plus retry, dead-letter, and ordering behaviour. Declared on a route as its default or on an individual output as an override.",
      "additionalProperties": false,
      "required": [
        "mode"
      ],
      "properties": {
        "mode": {
          "type": "string",
          "enum": [
            "direct",
            "queued"
          ],
          "description": "direct = publisher -> destination, no broker (does not survive an extended destination outage); queued = publisher -> durable provider -> delivery worker -> destination, at-least-once. queued requires the environment to declare a durableProvider with the matching capabilities (EVENT007 otherwise)."
        },
        "guarantee": {
          "const": "at-least-once",
          "description": "The only guarantee the platform sells: at-least-once delivery + stable event IDs + destination idempotency. Exactly-once is deliberately not offered."
        },
        "retry": {
          "type": "object",
          "description": "Retry behaviour before an event is given up on. Default when absent: 5 attempts with exponential backoff.",
          "additionalProperties": false,
          "properties": {
            "maxAttempts": {
              "type": "integer",
              "minimum": 1,
              "maximum": 100,
              "description": "Maximum delivery attempts per event before retries stop."
            },
            "backoff": {
              "type": "string",
              "enum": [
                "fixed",
                "exponential"
              ],
              "description": "Delay strategy between attempts: fixed = a constant interval, exponential = a growing interval."
            }
          }
        },
        "deadLetter": {
          "type": "object",
          "description": "What happens to events that exhaust their retries.",
          "additionalProperties": false,
          "properties": {
            "enabled": {
              "type": "boolean",
              "description": "Whether exhausted events are kept in a dead-letter queue instead of dropped. Default when absent: true for queued delivery, false for direct."
            },
            "retention": {
              "type": "string",
              "pattern": "^[0-9]+d$",
              "description": "DLQ retention in days, e.g. 14d."
            }
          }
        },
        "ordering": {
          "type": "object",
          "description": "Optional FIFO ordering, scoped to one output edge and one ordering key.",
          "additionalProperties": false,
          "required": [
            "mode",
            "key"
          ],
          "properties": {
            "mode": {
              "const": "fifo",
              "description": "FIFO is scoped to one output edge + one ordering key - never a global order. Different keys process concurrently."
            },
            "key": {
              "type": "string",
              "pattern": "^(subject|data\\.[^.\\s]+(\\.[^.\\s]+)*)$",
              "description": "The envelope field events are ordered by: `subject` or a data dot-path."
            },
            "onFailure": {
              "type": "string",
              "enum": [
                "block",
                "dead-letter-and-continue"
              ],
              "description": "Poison-message policy: block = a failed event blocks later same-key events (strict order over progress); dead-letter-and-continue = after retry exhaustion the event enters the DLQ and the ordering group continues. Default: dead-letter-and-continue."
            }
          }
        }
      }
    },
    "agentOutputTarget": {
      "type": "object",
      "description": "An agent destination: a named webhook handler on a target profile's gateway, with optional session and delivery policy.",
      "additionalProperties": false,
      "required": [
        "profile",
        "handler"
      ],
      "properties": {
        "profile": {
          "type": "string",
          "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
          "maxLength": 40,
          "description": "Target profile name. The compiler binds only to a webhook handler that profile's OWN contract declares - there is no fallback profile, ever."
        },
        "handler": {
          "type": "string",
          "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
          "maxLength": 40,
          "description": "The named webhook route on the target profile's gateway (config.yaml platforms.webhook routes)."
        },
        "session": {
          "$ref": "#/$defs/sessionPolicy",
          "description": "How this output's deliveries map onto Hermes sessions. Default when absent: per-event."
        },
        "delivery": {
          "$ref": "#/$defs/deliveryPolicy",
          "description": "Per-output override of the route's delivery default."
        }
      }
    },
    "routeOutput": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "agent": {
          "$ref": "#/$defs/agentOutputTarget",
          "description": "Deliver to an agent: a webhook handler on a target profile's gateway."
        },
        "chatops": {
          "$ref": "#/$defs/chatopsSpace",
          "description": "Deliver to a ChatOps space."
        },
        "delivery": {
          "$ref": "#/$defs/deliveryPolicy",
          "description": "Per-output override of the route's delivery default (chatops outputs)."
        }
      },
      "oneOf": [
        {
          "required": [
            "agent"
          ],
          "not": {
            "required": [
              "chatops"
            ]
          }
        },
        {
          "required": [
            "chatops"
          ],
          "not": {
            "required": [
              "agent"
            ]
          }
        }
      ],
      "description": "Exactly one destination: an agent (profile + handler) or a ChatOps space. Each output is an independent delivery edge with its own delivery ID, retry state, and receipt - one failing edge never blocks another."
    },
    "externalInputDecl": {
      "type": "object",
      "description": "One verified inbound webhook binding: an external sender POSTs to the shared gateway, which verifies the signature and normalizes the body into a typed event routes can consume.",
      "additionalProperties": false,
      "required": [
        "name",
        "event",
        "verification"
      ],
      "properties": {
        "name": {
          "type": "string",
          "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
          "maxLength": 40,
          "description": "DNS-label binding name. Becomes the opaque virtual path /v1/hooks/<binding-id> on the ONE shared webhook gateway - no per-profile public endpoint exists."
        },
        "event": {
          "$ref": "#/$defs/eventType",
          "description": "The versioned event type the normalized payload is published as."
        },
        "schema": {
          "type": "string",
          "pattern": "^[^/\\s][^\\s]*\\.schema\\.json$",
          "description": "Repo-relative path to the normalized payload's JSON Schema."
        },
        "subject": {
          "type": "string",
          "pattern": "^[^.\\s]+(\\.[^.\\s]+)*$",
          "description": "Dot-path into the normalized payload naming the business subject (same semantics as an output's subject field)."
        },
        "provider": {
          "type": "string",
          "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
          "description": "Optional provider normalizer (e.g. github). Absent = the generic normalizer: the verified body IS the event payload."
        },
        "verification": {
          "type": "object",
          "description": "Request-signature verification the gateway enforces on every delivery to this binding.",
          "additionalProperties": false,
          "required": [
            "type",
            "secretRef"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "hmac-sha256",
                "github-hmac-sha256"
              ],
              "description": "Signature scheme the gateway enforces. An unverifiable external input is rejected at compile - this field is required by schema, not just convention."
            },
            "secretRef": {
              "type": "object",
              "description": "Reference to the Kubernetes Secret holding the per-binding signing secret - a reference only, never the value.",
              "additionalProperties": false,
              "required": [
                "name",
                "key"
              ],
              "properties": {
                "name": {
                  "type": "string",
                  "minLength": 1,
                  "description": "Kubernetes Secret name holding the per-binding signing secret. The secret VALUE never appears in Git, plans, or receipts."
                },
                "key": {
                  "type": "string",
                  "minLength": 1,
                  "description": "Key within that Secret whose value is the signing secret."
                }
              }
            }
          }
        },
        "accepts": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "Optional allowlist of provider event kinds (e.g. issues.opened). Anything else is rejected at the gateway."
        }
      }
    },
    "endpoint": {
      "type": "object",
      "description": "A typed endpoint: a named port on a workload that should have an address, plus who may consume it. The compiler derives the canonical URL and any exposure from this declaration.",
      "additionalProperties": false,
      "required": [
        "name",
        "port",
        "type"
      ],
      "properties": {
        "name": {
          "type": "string",
          "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
          "maxLength": 40,
          "description": "DNS-label endpoint name. Becomes the first hostname label of the canonical URL. Uniqueness within the owning component is enforced by the compiler, not here - uniqueItems only rejects entries identical in every field."
        },
        "service": {
          "type": "string",
          "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
          "maxLength": 63,
          "description": "Kubernetes Service backing this endpoint. REQUIRED on app endpoints (the compiler cross-checks it against the rendered chart under doctor --deep); FORBIDDEN on agent endpoints, which are backed by the agent pod's own Service."
        },
        "port": {
          "type": "integer",
          "minimum": 1,
          "maximum": 65535,
          "description": "TCP port the endpoint is served on."
        },
        "path": {
          "type": "string",
          "pattern": "^/",
          "description": "Optional path prefix; defaults to /."
        },
        "type": {
          "type": "string",
          "enum": [
            "internal",
            "private",
            "authenticated",
            "public",
            "webhook",
            "external"
          ],
          "description": "Who consumes this endpoint: internal = same component, cluster DNS only, never exposed; private = other workloads in the environment; authenticated = a person, behind the identity provider; public = anyone, and ONLY ever explicit; webhook = an external event sender (signature declaration required); external = a named machine client under a service-token policy. Authentication is per-endpoint - v1's profile-level expose.access.policy has no v2 equivalent."
        },
        "signature": {
          "type": "string",
          "minLength": 1,
          "description": "Signature-verification scheme the receiver enforces (e.g. hmac-sha256). REQUIRED when type is webhook - an unverifiable webhook endpoint is rejected at compile (TOPO013)."
        },
        "provides": {
          "$ref": "#/$defs/capabilityName",
          "description": "Capability this endpoint provides. Consumers cite the capability name in requires[] instead of a literal URL; the compiler binds and injects the resolved URL."
        }
      },
      "if": {
        "properties": {
          "type": {
            "const": "webhook"
          }
        },
        "required": [
          "type"
        ]
      },
      "then": {
        "required": [
          "signature"
        ]
      }
    },
    "agentEndpoint": {
      "$ref": "#/$defs/endpoint",
      "description": "The endpoint shape with service forbidden: agent endpoints are backed by the agent pod's own Service.",
      "not": {
        "required": [
          "service"
        ]
      }
    }
  },
  "properties": {
    "apiVersion": {
      "const": "hermes-gitops.factorylevel.dev/agent-team/v1alpha1",
      "description": "Pins the agent-team contract this document is read under; the CLI refuses versions it does not implement."
    },
    "kind": {
      "const": "Endpoints",
      "description": "Document kind - always Endpoints."
    },
    "endpoints": {
      "type": "array",
      "uniqueItems": true,
      "items": {
        "$ref": "#/$defs/agentEndpoint"
      },
      "description": "The agent's own endpoints (backed by the agent pod - no service field). Replaces v1's expose.services with typed entries; a v1 file's expose block is projected onto this shape during adaptation."
    },
    "externalInputs": {
      "type": "array",
      "description": "Verified external webhook bindings this profile declares. Routes consume them via from.externalInput.",
      "uniqueItems": true,
      "items": {
        "$ref": "#/$defs/externalInputDecl"
      }
    },
    "routes": {
      "type": "array",
      "uniqueItems": true,
      "items": {
        "type": "object",
        "description": "One route from an external input THIS agent receives: optional equality filters, a delivery default, and one or more destinations. Routes from app outputs live in harness-hg/apps.yaml under the producing app, never here.",
        "additionalProperties": false,
        "required": [
          "name",
          "from",
          "outputs"
        ],
        "properties": {
          "name": {
            "type": "string",
            "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
            "maxLength": 40,
            "description": "DNS-label route name, unique within the file."
          },
          "from": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "externalInput"
            ],
            "description": "The route's single event source - an entry in this file's externalInputs.",
            "properties": {
              "externalInput": {
                "type": "string",
                "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
                "maxLength": 40,
                "description": "An entry in externalInputs of THIS file."
              }
            }
          },
          "filter": {
            "type": "object",
            "minProperties": 1,
            "propertyNames": {
              "pattern": "^(subject|data\\.[^.\\s]+(\\.[^.\\s]+)*)$"
            },
            "additionalProperties": {
              "type": [
                "string",
                "number",
                "boolean"
              ]
            },
            "description": "Equality filters over declared normalized fields (`subject` or data dot-paths). A route delivers only events matching every entry. Filters select among declared routes - a payload can never CREATE a route."
          },
          "delivery": {
            "$ref": "#/$defs/deliveryPolicy",
            "description": "The route's delivery default; individual outputs may override."
          },
          "outputs": {
            "type": "array",
            "minItems": 1,
            "items": {
              "$ref": "#/$defs/routeOutput"
            },
            "description": "The destinations this route delivers to; each entry is an independent delivery edge."
          }
        }
      },
      "description": "Routes whose source is one of this file's externalInputs. The compiler folds them into this agent's communication routes; a route not declared here does not exist."
    },
    "expose": {
      "type": "object",
      "description": "HERMES AGENTS ONLY, and only while the destination still deploys agent Service ports from the record's expose block (the endpoints ApplicationSet generator is staged, not promoted): the services the agent pod publishes and the access policy the tunnel ingress applies. Folded verbatim into the HermesProfile record so the rendered record is byte-identical to the legacy contract-v3 authoring. The compiler refuses this block on an Eve agent. Retired together with the legacy generator.",
      "additionalProperties": false,
      "required": [
        "services"
      ],
      "properties": {
        "services": {
          "type": "array",
          "description": "The exposed services. Each becomes a named port on the instance's Service and a routing rule on the configured ingress provider.",
          "minItems": 1,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "name",
              "port"
            ],
            "properties": {
              "name": {
                "type": "string",
                "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
                "description": "DNS-label service name; becomes the port's name on the instance's Kubernetes Service."
              },
              "port": {
                "type": "integer",
                "minimum": 1,
                "maximum": 65535,
                "description": "TCP port on the agent pod this service targets."
              },
              "path": {
                "type": "string",
                "pattern": "^/",
                "description": "HTTP path prefix routed to this service. Default when absent: /."
              }
            }
          }
        },
        "access": {
          "type": "object",
          "description": "Access control for the exposed services under the Cloudflare tunnel ingress.",
          "additionalProperties": false,
          "properties": {
            "policy": {
              "type": "string",
              "enum": [
                "service-token",
                "idp",
                "mixed"
              ],
              "description": "Who may reach the exposed services: service-token = named machine clients, idp = people via the identity provider, mixed = both. Default when absent: service-token."
            }
          }
        }
      }
    }
  },
  "patternProperties": {
    "^x-": {
      "description": "YAML anchor scratch space: a top-level key starting with `x-` is IGNORED by every reader and exists only so a repeated block (a delivery policy, a values fragment) can be declared once and referenced with `*alias`. Nothing under it is a declaration."
    }
  }
}
